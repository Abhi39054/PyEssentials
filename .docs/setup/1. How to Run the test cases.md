# How to Run test cases 

Make sure that your package is installed in the python env that you are using.


You can install the package using below commands. Make sure you are outside the tests/ directory.

    pip install -e .

Once install you can run below commands to check the test cases.

    pytest